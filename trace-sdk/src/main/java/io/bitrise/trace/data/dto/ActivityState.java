package io.bitrise.trace.data.dto;

/**
 * Different Activity states that an Android Activity can have.
 */
public enum ActivityState {
    CREATED,
    STARTED,
    RESUMED,
    PAUSED,
    STOPPED,
    RESTARTED
}
